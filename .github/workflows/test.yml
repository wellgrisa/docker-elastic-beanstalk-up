name: Api

on:
  push:
    branches: ["include-lerna"]

  workflow_dispatch:

jobs:
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    environment: docker-elastic-beanstalk-up
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - run: yarn

      - name: Version
        run: yarn lerna version --conventional-commits --no-commit-hooks --yes

      # - name: Configure AWS
      #   id: configure-aws
      #   uses: ./.github/actions/configure-aws
      #   with:
      #     role-to-assume: ${{ secrets.ROLE_TO_ASSUME }}

      # - name: Sending API Docker image
      #   env:
      #     REGISTRY: ${{ steps.configure-aws.outputs.ecr-registry }}
      #     REPOSITORY: docker-elastic-beanstalk-up
      #   run: yarn lerna run build-docker --ignore models --since

      # - name: Deploy🤞
      #   run: yarn deploy
